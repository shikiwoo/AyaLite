[project]
name = "ayalite"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shiki", email = "shiki@shikiwoo.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ayalite = "ayalite:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
